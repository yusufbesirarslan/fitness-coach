.env
__pycache__/
instance/
*.db
*.pyc
.pytest_cache/
.pytest-basetemp/
.venv/
venv/

# Bedrock / AWS uzun ömürlü anahtarlar & kimlik dosyaları — ASLA commit etme
.env.*
!.env.example
# Staging template. Names only, never values — enforced by
# tests/test_staging_separation.py, which fails if a filled-in secret,
# a production identifier or an enabled Sprint 14 flag appears in it.
!.env.staging.example
*.csv
*-api-key*.csv
*accessKeys*.csv
*credentials*.csv
*.pem
*.key
*.pfx

# Coverage artifacts
.coverage
.coverage.*
htmlcov/
coverage.xml

# SAM build artifacts (infra/)
.aws-sam/

# Local isolated feature worktrees
.worktrees/

# Sprint 0 local browser-audit outputs
.audit-venv/
artifacts/ui-audit/
instance/axis_frontend_audit.db
