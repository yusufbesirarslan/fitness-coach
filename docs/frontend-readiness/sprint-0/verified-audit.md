# Verified Audit

Status: **supported-environment run complete (2026-07-23)**. The mandatory Chromium preflight and the full 325-capture run both succeeded in WSL Ubuntu-24.04; the runtime-environment blocker that previously held Sprint 0 open is cleared.

The supplied audit is preserved unchanged and represented by 59 canonical material findings in `finding-map.json`. Extraction uses rules version `1.0.0`; repeated executive-summary, screen, priority, and backlog wording maps to one finding. The source SHA-256 is `302aeaf22f9d834f41216ee420b5d1201b987f26b77715b4b67da124b5118b92`.

Static repository review confirms three architecture observations: competing mobile navigation layers (`EXT-008`), inconsistent destination vocabulary (`EXT-011`), and structural emoji usage (`EXT-026`). These are source-level confirmations, not browser render claims. Four questions remain product decisions (`EXT-003`, `EXT-004`, `EXT-005`, `EXT-009`, plus social/menu/wearable direction records). All render-, interaction-, accessibility-, and state-dependent claims remain `BLOCKED_BY_MISSING_ENVIRONMENT`; none were promoted using unsupported-host output.

The supported run executed inside WSL Ubuntu-24.04 (Linux, `supported: true`) using Playwright 1.61.0 and its version-coupled Chromium (revision 1228, major 149). The preflight launched the process, navigated loopback, wrote a screenshot, and shut down cleanly (`success: true`). `capture --tier full` then captured all 277 mandatory Chromium captures with zero failures across the 25-route inventory; the 48 optional WebKit/Firefox captures are `blocked` because those browsers are not installed here (their system libraries need privileged apt access). `verify --manifest` reported `audit artifacts valid`. See `environment-diagnostics.json` and the curated evidence under `evidence/2026-07-23/`.

Automated per-capture checks over the 277 Chromium captures (raw signal, not yet finding-by-finding adjudicated): 0 unresolved template placeholders and 0 uncaught page errors; horizontal overflow on 22 captures across 5 routes (notably nutrition-empty and dashboard rest-day at narrow widths); fixed-bottom occlusion on 152 captures across 15 routes (consistent with the competing mobile navigation / safe-area hypotheses, `EXT-008`); console errors on 22 captures across 4 routes. The 268 `failed_requests` captures largely reflect the isolated audit factory blocking external CDN/analytics requests rather than application defects, and should be triaged against the allowlist before promotion. The render-, interaction-, and state-dependent external findings are now UNBLOCKED by this evidence; adjudicating each blocked finding to a confirmed or non-reproduction result against these screenshots and result JSONs is the first Sprint 1 analytical task and is intentionally not asserted here.

The frozen test baseline remains 48 passed with 343 `datetime.utcnow()` deprecation warnings under the exact command and environment in `warning-baseline.json`. Sprint 0 does not attempt broad datetime remediation.
