# Tüm bağımlılıklar tam sürüme (==) sabitlenmiştir → tekrarlanabilir (reproducible)
# build. `>=` aralıkları her derlemede o anki EN YENİ sürümü çekiyordu; pin'ler bunu
# dondurur, böylece gelecekte açık çıkan/breaking bir sürüm deploy'a sessizce sızmaz.
# Sabitlenen sürümler mevcut test paketiyle (pytest) doğrulanan setdir. Yükseltme
# bilinçli yapılır: sürümü burada güncelle, `pytest` çalıştır, commit'le.
Flask==3.1.3
Flask-Login==0.6.3
Flask-Limiter==4.1.1
Flask-Migrate==4.1.0
Flask-SQLAlchemy==3.1.1
Authlib==1.7.2
SQLAlchemy==2.0.52
Werkzeug==3.1.8
gunicorn==26.2.0
openai==2.50.0
anthropic[bedrock]==0.120.2
psycopg2-binary==2.9.12
python-dotenv==1.2.3
requests==2.34.2
Brotli==1.2.0
beautifulsoup4==4.15.0
# Güvenilmeyen yükleme (PDF/görsel) ayrıştırıcıları — Dependabot (.github/dependabot.yml)
# yükseltme PR'ları açar; pin'ler tekrarlanabilir build için sabit kalır.
pdfplumber==0.11.10
Pillow==12.3.0
boto3==1.43.81
redis==8.1.0
# Arka-plan iş kuyruğu (Sprint 4 WS8) — app/jobs/, worker.py. Redis yoksa/rq
# kurulu değilse get_queue() None döner ve işler satır-içi (inline) çalışır.
rq==2.11.0
# Merkezi e-posta altyapısı (app/services/email_service.py) — Resend resmi SDK.
resend==2.42.0
# Hata izleme — yalnızca SENTRY_DSN ayarlıysa etkin (app/observability.py).
sentry-sdk[flask]==2.68.1
cryptography==50.0.1
tzdata==2026.3
