{
  "mcpServers": {
    "fitx-coach": {
      "command": "python",
      "args": ["-m", "fitx_mcp"],
      "cwd": "${PROJECT_ROOT}",
      "env": {
        "DATABASE_URL": "${DATABASE_URL}"
      }
    }
  }
}
