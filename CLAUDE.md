# Fitness Coach (FitX)

- Weekly-program istemci entegrasyonu (Sprint 6 PR6.2) ? varsay?lan KAPALI `WEEKLY_PROGRAM_UI_ENABLED` a??ld???nda mevcut `window.FitXWeeklyProgram.init(root)->boolean` s?n?r? tek `GET /api/training/weekly-program` ?a?r?s?yla salt-okunur kart? y?kler. Durumlar: idle/loading/populated/insufficient_data/missing_baseline/error/malformed; retry e?zamanl? iste?i engeller, eski i?eri?i loading ile temizler ve request-generation korumas? bayat tamamlanmay? engeller. Payload kanonik planner enumlar?/booleanlar/s?ral? reason+explanation anahtarlar?/?ift null veya pozitif hacimler/ger?ek ISO tarih i?in do?rulan?r; bilinmeyen top-level alanlar ve string `unsupported` de?erleri eklemeli evrim olarak kabul edilir. ?stemci HEDEF/PENCERE/DELTA hesaplamaz; tarih ve delta g?stermez. DOM yaln?zca createElement/textContent/replaceChildren ile kurulur. `weekly_program.*` TR/EN copy global `window.I18N`'den ??kar?l?r, yaln?zca ON mount i?inde g?venli application/json + tojson blokla verilir; OFF yol feature copy/markup/script/request/bo?luk almaz. CSS yaln?zca `.weekly-program-*`, 640 px breakpoint + ortak card/btn/skeleton/token kullan?r. Sprint 6 PR6.3 (TAMAM) bu sistemi production'a sertlestirdi: endpoint yaniti `Cache-Control: private, no-store` tasir (route-scoped after_request); gozlemlenebilirlik PII'siz `[TRAINING][WEEKLY_PROGRAM] request_id=... state={neutral|missing_baseline|populated|error}` satirina donustu (metrik terfisi ertelendi, handoff'ta CloudWatch sorgusu var); activeRequest/requestGeneration mount-basi context'e tasindi + `mount.isConnected` kontrolu bayat/kaldirilmis-mount tamamlanmasini yok sayar; retry sonrasi odak yonetimi (basari=heading tabindex=-1, hata=retry butonu); dort-yonlu runtime flag matrisi test edildi (iki bayrak birbirini etkilemez); AST/kaynak yapisal muhafizlar istemcide hedef/pencere aritmetigi, planner adi, ikinci endpoint, storage/polling API'lerini reddeder; endpoint hala tek history SELECT, yazma yok. Detay + production-readiness denetimi: docs/handoff.md -> Sprint 6 PR6.3.
- UIUX Sprint 1 PR3 — Plan Experience + Coach Destination Hardening (iki bağımsız, varsayılan KAPALI sunucu-sahipli bayrak: `UIUX_PLAN_V2_ENABLED`, `UIUX_COACH_PAGE_V2_ENABLED`; request-zamanında `current_app.config`'ten okunur, query/cookie/header/storage'dan ASLA; her biri tek başına geri alınır). **Plan V2** (`/training`): legacy `training.js`'in YASAK istemci yetkisini (saate dayalı "bugün" seçimi, dinlenme çıkarımı, localStorage tamamlanma) sunucu-otoriter bir görünümle değiştirir — saf `app/plan_presenter.py` (frozen dataclass'lar, kanonik state-id sabitleri, action'lar label KEY taşır) + salt-okunur `app/services/plan_facts.py` (kanonik `get_active_plan` selektörünü yeniden kullanır; plan_data'yı sunum-amaçlı sıralı gün→egzersiz yapısına GÜVENLİ ayrıştırır). İKİ AYRI durum modeli: **sayfa** durumu (`read_error`/`no_active_plan`/`active_plan`/`partial_active_plan`) ve ondan bağımsız **haftalık bölüm** durumu (`loading`/`populated`/`missing_baseline`/`insufficient_data`/`partial`/`error`/`disabled`). İkincil haftalık arıza sayfayı ÇÖKERTMEZ; `WEEKLY_PROGRAM_UI_ENABLED=0` → bölüm `disabled` (mount/script/istek YOK, aktif plan görünür kalır). populated'da baskın CTA YOK (plan içeriği hedefin kendisi); no_active_plan mevcut kanonik in-page üreticiyi kullanır (`/training`'e SELF-LINK YOK); read_error güvenli yeniden-dene (`fxReload`). `templates/plan.html` legacy `training.js`'i ASLA yüklemez; günler native `<details>`; haftalık `weekly_program.js` tüketicisi olduğu gibi yeniden kullanılır (training.html bağımlılığı yok). **Coach Page V2** (`/coach`): AYNI yüzen widget'ı (`coach_widget.js`) yeniden kullanır — İKİNCİ koç uygulaması YOK. Yaşam döngüsü sahipliği: modül-seviyesi `__cwWidgetInit` guard + var-olan `#cw-root`'u benimseme → ikinci script değerlendirmesi temiz no-op (tek erişilebilir örnek/composer/stream, çift `/coach/history` fetch yok). Route-modu davranışları (auto-open, "Koçu aç" yedeği `axCoachOpen` — toggle YALNIZCA kapalıyken → idempotent açılış) YALNIZCA `coach_v2.html` şablonunda yaşar; yüzen widget her yerde değişmez. AI sistem/adaptif promptu, model/sağlayıcı, streaming protokolü, konuşma kalıcılığı, rate-limit/entitlement/auth/moderation DEĞİŞMEDİ; CSP nonce/CSRF/çıktı-escape korunur. Legacy `training.html` (Plan OFF) 320px yatay taşması (24px EN/36px TR; base 9400641'de var olan) sunum-amaçlı, legacy'e özgü dar CSS düzeltmesiyle giderildi: `.wstats` 380px altında iki sütuna iner (`training.css`; `.wstats` başka şablonda yok → Plan V2/Nav/Today etkilenmez). Kanıt + rollout/rollback: docs/handoff.md -> UIUX Sprint 1 PR3.


Flask + SQLAlchemy + Bedrock/Claude Sonnet (primary heavy AI path) + OpenAI
(fallback/light paths, `gpt-4o-mini`) + FatSecret API proxy + Cognito (native
backend API — Hosted UI/OIDC KAPALI) + S3 avatar/meal image storage + Redis.
Deploy: tek AWS EC2 üzerinde Docker Compose (önceden Railway'deydi).

## Yapı
- starter.py — ince giriş noktası (`gunicorn starter:app`); asıl uygulama app/ paketinde
- app/__init__.py — application factory (create_app), blueprint kaydı
- app/config.py — env/config yükleme, güvenlik ayarları (cookie, SECRET_KEY, TLS zorunluluğu)
- app/hooks.py — CSRF koruması, CSP başlığı + per-request nonce, streak/rollover hook'ları
- app/extensions.py — db, migrate, login_manager, limiter, redis_client, openai_client
- app/models.py — tüm SQLAlchemy modelleri
- app/db_init.py — boot'ta create_all + bekleyen Alembic migration'ları otomatik uygular + idempotent legacy ALTER'lar (FITX_SKIP_DB_INIT=1 ile atlanır)
- app/cli.py — flask CLI komutları (seed-quests, weekly-reset)
- app/timeutil.py — TEK gün/saat kaynağı: sabit Europe/Istanbul (app_today/day_key/utc_day_bounds). Tüm gün anahtarları buradan; doğrudan date.today()/utcnow().strftime("%d.%m") KULLANMA
- app/blueprints/ — auth, profile, nutrition, food, menu, training, tracking, social, gamification, supplements, coach, notifications (Sprint 5 PR1: sosyal bildirim uçları /notifications*; prefix yok), challenges (Sprint 5 PR3: /challenges + /challenges/data + /challenges/<id>/{join,leaderboard}; prefix yok). social ayrıca Feed V2 (Sprint 5 PR2): /feed/data cursor keyset, /feed/repost (repost/quote), /feed/item/* (like/comment/sil), /pump-check/*/comments sayfalama+silme, /feed/{hide,unhide,report} moderasyon. gamification /leaderboard/data artık resetAt (Pazar 23:59 Istanbul → UTC) döner — geri sayım UTC↔Istanbul kayması giderildi (Sprint 5 PR3; docs/LEADERBOARD.md)
- app/services/ — ai, ai_coach, ai_nutrition, calculations, fatsecret, foodcache, gamification, premium, referral, cognito_service (native cognito-idp sarmalayıcı; eski cognito.py/cognito_idp.py Sprint 3'te SİLİNDİ — Hosted UI/OIDC yok), cognito_jwt (uygulamadaki TEK JWT doğrulayıcı; cognito_service ona delege eder), session_store (Fernet ile şifreli sunucu-tarafı Cognito token deposu), avatars, injury_constraints, menu_extract/fetch/ocr, training_generation/, validators, email_service (merkezi Resend e-posta altyapısı — SDK'ya yalnızca bu modül dokunur; RESEND_API_KEY yoksa no-op), email_templates (markalı auth e-posta şablonları — saf stdlib, Lambda kopyasıyla bayt-eş tutulur: tests/test_email_templates_sync.py), friends (Sprint 5 PR1: get_friend_ids/are_friends TEK kaynağı — pump_checks/gamification/social buraya delege eder), notifications (Sprint 5 PR1: notify session-add-no-commit + okunmamış-dedup + mark_read + purge_old; docs/NOTIFICATIONS.md), feed (Sprint 5 PR2 Feed V2: 3 keyset kaynağını —PumpCheck/FeedItem/Activity-milestone— sorgu-zamanında birleştirir; cursor codec + _rank algoritmik dikişi + kind-başı serileştirme + engagement.target sözleşmesi; repost yalnızca visibility=='feed' içeriğe/kitle genişletmez; docs/FEED.md), challenges (Sprint 5 PR3: record_event TEK olay hunisi —tüm quest olayları _claim_quest'ten + pump_check_created/active_day/xp_earned doğrudan— session-add-no-commit; hesaplı ISO-hafta period_key = current_challenge_week/period_end_utc, Pazar 23:59 Istanbul sınırı; global auto get-or-create begin_nested+IntegrityError guard, featured opt-in; korumalı UPDATE ile tam-bir-kez tamamlama → XP+rozet+bildirim+feed; count_challenge_xp=False özyineleme kesici; docs/CHALLENGES.md), badges (Sprint 5 PR3: BADGE_CATALOG + award_badge session-add-no-commit + badges_for)
- AI yanıt hattı (Sprint 4 WS3, modüler aşamalar): ai_pipeline (orkestratör; kanonik bileşim) → context_builder (koç bağlam blokları + FRIEND_DATA fence) → memory_manager (kalıcı konuşma hafızası + kısa-dönem geçmiş sınırlama + estimate_tokens) → prompt_builder (system/messages montajı; BEDROCK_PROMPT_CACHE parametreyle gelir) → sağlayıcı döngüleri (ai_coach'ta kaldı: araçlar, staging→commit, Bedrock/OpenAI) → moderation (girdi kapısı MAX_QUESTION_CHARS + çıktı kancası) → response_formatter (hata-yedeği kararı, COACH_FALLBACKS). ai_coach eski adları (_fetch_coach_context, _sanitize_client_history, _COACH_FALLBACKS...) re-export eder — mevcut import yolları ve test monkeypatch yüzeyi değişmedi
- Kalıcı koç hafızası (Sprint 4 WS1): CoachConversation + CoachMessage tabloları; /ask her turu DB'ye yazar (hata-yedeği yanıtlar HARİÇ — B16), bağlam penceresi en yeni mesajlardan geriye AI_CONTEXT_TOKEN_BUDGET dolana dek kurulur, özetlenmemiş kuyruk AI_SUMMARY_TRIGGER_TOKENS'ı aşınca eski turlar tek hafif LLM çağrısıyla conversation.summary'ye katlanır (mesajlar SİLİNMEZ, pencere dışı kalır). `GET /coach/history` geçmişi, `POST /coach/conversation/reset` aktif konuşmayı ARŞİVLER. Hafıza katmanının HER adımı arızaya dayanıklıdır: çökerse eski client-history yoluna düşülür, sohbet kırılmaz. Kapatma anahtarı: AI_MEMORY_ENABLED=0
- Akışlı koç yanıtı (Sprint 4 WS2): `POST /ask/stream` bloklayıcı /ask'in yanında durur (SSE çerçeveleri: meta → delta* → done | error). app/services/ai_stream.py gerçek Bedrock akışlı araç döngüsünü (bedrock_client.messages.stream()) koşar; ai_pipeline.stream_answer sarar — done'da turu kalıcılaştırır (B16: hata-yedeği HARİÇ), istemci akış ortasında koparsa (GeneratorExit) kısmi yanıtı interrupted=True kaydeder. Sağlayıcı geçişi B-kuralına tabi: Bedrock→OpenAI yalnızca İLK delta istemciye gitmeden VE hiç araç yan etkisi olmadan; sonrasında akış-ortası hata dostça i18n `error` çerçevesi döndürür (sağlayıcı istisna metni ASLA sızmaz). ai_stream_concurrency_gate slotu yanıt KAPANANA dek tutar (call_on_close) — normal kapı view döndüğünde bırakır, akışta bu ilk token'dan öncedir, yani stream'leri hiç sınırlamazdı. Frontend (static/coach_widget.js): fetch POST + ReadableStream SSE okuyucu (EventSource X-CSRFToken gönderemez), AbortController ile Durdur, Yeniden üret, marked+DOMPurify markdown (jsdelivr pinned + SRI), açılışta GET /coach/history hidrasyonu
- AI önbellek/kurtarma/kısıt katmanı (Sprint 4 WS5/WS9/WS7): app/services/ai_cache.py jenerik Redis önbelleği (cache_get/cache_set, anahtar `ai:cache:<feature>:v1:<sha256>`, JSON değer, özellik-başı TTL, foodcache deseni: Redis yoksa no-op, hatalar yutulur). Deterministik TR→EN besin-adı normalizasyonuna bağlı (ai_nutrition._normalize_food_query_en + batch); yalnızca BAŞARILI sonuç önbelleklenir. app/services/ai_recovery.py kurtarma merdiveni (_heavy_chat'e bağlı): geçici hata (TransientAIError — _claude_chat/_openai_chat rate-limit/timeout/bağlantı için fırlatır, RuntimeError alt sınıfı) → jitter'lı retry; kalıcı hatada Bedrock→OpenAI; İKİSİ de düşerse son-iyi (last-good) Redis yanıtı (içerik-hash anahtarlı — iki kullanıcı yalnızca birebir aynı girdide paylaşır, sızıntı yok); o da yoksa mevcut dostça RuntimeError. Bedrock SDK max_retries 2→1 (BEDROCK_MAX_RETRIES) — katmanların çarpımını önler. WS7: `/ask*` üzerinde AI_BURST_RATELIMIT="5 per minute" (AI_RATELIMIT üstüne) + kullanıcı-başı arıza soğuması (AI_FAILURE_THRESHOLD ardışık arıza → Redis NX anahtar AI_FAILURE_COOLDOWN_SECONDS boyunca 429+Retry-After, KOTADAN ÖNCE; ilk başarıda sayaç sıfırlanır). Hepsi varsayılan AÇIK ve Redis'siz zarifçe no-op
- Arka-plan işler + gözlemlenebilirlik (Sprint 4 WS8/WS6): app/jobs/ — RQ iş kuyruğu. get_queue() None-güvenli (rq/Redis yoksa None), enqueue_or_run(func,...) worker varsa kuyruğa atar yoksa SATIR-İÇİ çalıştırır (worker OPSİYONEL — yoksa işler inline'a düşer, uygulama çalışır). worker.py entrypoint (Linux Worker / Windows SimpleWorker), compose 'worker' servisi, heartbeat daemon thread. İlk bağlı task: summarize_conversation (PR2'nin inline özetlemesi artık ai_pipeline._memory_stage'de enqueue). Ölü-mektup: `flask rq-failed`/`rq-requeue`. /health?deep=1 worker alanı BİLGİLENDİRİCİ (gating değil). rq==2.10.0. WS6: app/services/ai_metrics.py CloudWatch FitX/AI metrikleri (AITurn/AIErrors/*Tokens/SummarizeJob), AI_METRICS_ENABLED VARSAYILAN 0 (cloudwatch:PutMetricData izni gerekir), kapalı/boto3 yokken tam no-op. İzleme: sunucu-üretimi 16-hane request_id → logfmt satırı + /ask/stream SSE meta + Sentry tag. Docs: docs/{AI_ARCHITECTURE,MEMORY,STREAMING,OBSERVABILITY,RATE_LIMITING,DEPLOYMENT}.md. Yük testleri tests/load/ (@pytest.mark.load, pytest.ini `-m "not load"` ile varsayılan atlanır; `-m load` ile koş)
- app/prompts/ — TÜM LLM istem şablonları (Sprint 4 WS4): system (koç sistem promptu + dil direktifi + Sprint 6 PR4 ADAPTIVE_COACH_SYSTEM_PROMPT varyantı), goals (plan-koçluğu), nutrition (besin arama/EN normalizasyon/menü çıkarımı/porsiyon-makro/öğün toplamı + PORTION_SANITY_RULE), workout (training_generation'a delege), progress (haftalık check-in). Yeni LLM istemi eklerken şablonu BURAYA koy, servise gömme; modüller saf string üretir (DB/Flask/istemci import etmez)
- infra/cognito-email-sender/ — Cognito CustomEmailSender Lambda + KMS (SAM): doğrulama/sıfırlama kod e-postalarını Resend üzerinden markalı gönderir; havuza bağlama runbook'u README'sinde (mimari: docs/auth-emails.md)
- fitx_mcp/ — MCP sunucusu (AI Coach DB araçları). DİKKAT: araçlar user_id'yi parametre alır, kendi yetkilendirmesi YOKTUR — yalnızca stdio/in-process kullan; HTTP taşıması FITX_MCP_ALLOW_HTTP=1 + loopback arkasındadır, asla public proxy'e koyma
- app/services/training_history/ — Adaptive Training Engine TEMELİ (Sprint 6 PR1): WorkoutLog geçmişine TEK kanonik, deterministik, ORM tabanlı erişim + ilerleme temel hesapları. Saf/kirli ayrımı (queries.py=DB okuma, analysis.py=saf hesap, models.py=frozen value objects). Istanbul-gün pencereleri app/timeutil'den, WORKOUT_COMPLETION_MARKER hariç tutma tek yerden (fetch_workout_entries/is_completion_marker). HAFTALIK PENCERE GEOMETRİSİNİN TEK SAHİBİ weekly_windows: pencereler GERİYE DÖNÜK ("trailing") — en yeni pencere end_day'de BAŞLAMAZ, end_day'de BİTER ([end_day-6, end_day]). Üst katmanlar (progression/planning/weekly_program) bunu besler, kendi penceresini TÜRETMEZ. (2026-07-22 düzeltmesi: eski ileri-bakan [today, today+6] penceresi yalnızca BUGÜNÜN kayıtlarını tutup kendini "hafta" gibi sunuyordu → tek günlük antrenman haftalık hacim sanılıyordu ve deload dinlenme gününde hiç tetiklenmiyordu; NEEDED_FIXES.md #5.) build_training_history_summary orkestratör. time_series_model.build_performance_history + ai_coach._today_workout_totals + tracking.py /api/progress/workout (Sprint 6 PR3) buna delege eder (davranış AYNI); tracking.py heatmap/insights + fitx_mcp + analytics_engine kendi inline okuyucularını hâlâ tutar (sonraki PR). docs/TRAINING_HISTORY.md
- app/services/training_progression/ — Adaptive Training Engine İLERLEME KATMANI (Sprint 6 PR2): training_history TEMELİni deterministik ilerleme SİNYALLERİNE çevirir (hacim/kuvvet trendi, plato, deload, tutarlılık + koçun sıradaki tek sinyali). Saf yorum katmanı — pencereleme/marker mantığını YENİDEN türetmez, training_history'yi besler (bağımlılık tek yönlü; temel bu katmanı import ETMEZ). analysis.py=saf sinyal fonksiyonları (eşikler açık sabit), models.py=frozen ProgressionReport/WeeklyStrength, __init__.build_progression_report orkestratör. Est-1RM Epley temel bloğu (PR1'de kullanılmıyordu) artık kuvvet trendinde tüketiliyor. Güvenilir hesaplanamayan kavram → açık NÖTR değer (spekülatif sezgi değil). ŞEMA/route/koç-prompt/UI değişikliği YOK — runtime bağlama sonraki PR. docs/TRAINING_PROGRESSION.md
- app/services/training_planning/ — Adaptive Training Engine PLANLAMA KATMANI (Sprint 6 PR3): ProgressionReport'un next_signal'ını deterministik haftalık plan önerisine çevirir (frozen AdaptivePlan: week_focus/volume_action/intensity_action/volume_delta_pct/overload_ready/maintenance_recommended + sıralı reason_codes + gömülü ProgressionReport). next_signal ile 1:1 eşleme — İKİNCİ öncelik zinciri YOK (ham boolean'lardan karar türetme). Sabitler açık: VOLUME_INCREASE_STEP=0.05, DELOAD_VOLUME_CUT=0.40; yoğunluk büyüklüğü BİLEREK modellenmez (egzersiz-başı veri yok). plateau→maintenance (hold/hold; yorgunluk verisi olmadan itmek riskli), keep_pushing→steady. Bağımlılık tek yönlü: planning→progression→history. training_generation (LLM plan üretici) ile İLGİSİZ. Bu PR'da tracking.py /api/progress/workout foundation'a converge edildi (bayt-eş, characterization testli); heatmap/insights hâlâ inline (sonraki PR). docs/TRAINING_PLANNING.md
- app/services/adaptive_plan_context.py — Sprint 6 PR4 AI Coach için AdaptivePlan'ın TEK prompt-serileştirme sınırı: versioned/deterministik minimal JSON, salt-okunur koç politikası, Exception→tam nötr kontrat, varsayılan KAPALI AI_ADAPTIVE_PLAN_CONTEXT rollout/rollback kapısı. Koç karar türetmez; blocking/streaming ve OpenAI/Bedrock aynı context_builder bloğunu tüketir. Bayrak AÇIKken koç sistem promptu da ADAPTIVE_COACH_SYSTEM_PROMPT'a döner (app/prompts/system.py): eski "yorgunluk ≥4 → hacmi/şiddeti düşür" türü yetkiler kaldırılır, AdaptivePlan TEK planlama kaynağıdır. Bu seçim bağlam METNİNDEN değil bayraktan gelir (ai_coach._adaptive_plan_context_enabled → prompt_builder(adaptive_plan_context=...)) — kullanıcı yazdığı bir alan kanonik başlığı taklit edip promptu çeviremesin. Bağımlılık yalnızca yukarı yönlüdür; docs/TRAINING_PLANNING.md
- app/services/weekly_program/ — Adaptive Training Engine HAFTALIK PROGRAM TÜKETİCİSİ (Sprint 6 PR5): AdaptivePlan'ı gelecekteki UI/runtime'ın sunabileceği yapısal haftalık öneriye çevirir (frozen WeeklyProgramRecommendation). ÇEVİRİ katmanı, İKİNCİ planlama motoru DEĞİL: tüm karar alanları (week_focus/volume_action/intensity_action/volume_delta_pct/overload_ready/maintenance_recommended/reason_codes) plandan BİREBİR kopyalanır; ilerleme/plato/deload/overload kararı burada TÜRETİLMEZ. Tek aritmetik: baseline = planın gömülü progression.weekly_volume serisindeki EN SON pozitif hacim (sıfır-hacimli haftalar atlanır — dinlenme haftası ölçüm değil, veri yokluğudur), target = round(baseline*(1+volume_delta_pct), 2); pozitif hacim yoksa İKİSİ de None (asla 0.0). AdaptivePlan'ın modellemediği şeyler (session_frequency/intensity_magnitude/exercise_selection) UNSUPPORTED_CAPABILITIES ile açıkça "desteklenmiyor" döner — sezgi uydurulmaz. explanation_keys yalnızca mevcut kanonik kodların ön-ekli hâlidir (İKİNCİ sözlük YOK, TR metin YOK). Kendi geçmişini OKUMAZ (WorkoutLog/app.models/app.extensions/Flask/json importu yok); JSON serileştirme TEK sahibi adaptive_plan_context'te kalır. Bağımlılık tek yönlü: weekly_program→planning→progression→history. TEK runtime yüzeyi (PR5 ikinci bölüm): salt-okunur `GET /api/training/weekly-program` (app/blueprints/training.py:get_weekly_program) — @require_auth, tek build_weekly_program çağrısı, payload.py ile saf dict projeksiyonu, jsonify. Route İNCE: sorgu yok, eşik yok, WorkoutLog okumaz (view'in AST'si testte doğrulanır). weeks/end_day servis varsayılanına SABİTLENİR, query string'den OKUNMAZ — analiz penceresi bir planlama düğmesidir, HTTP parametresi değil. baseline_weekly_volume TAM bir geriye dönük 7 günlük pencerenin toplamıdır (tek seans değil) — bkz. weekly_windows trailing geometrisi. HATA SEMANTİĞİ: planlayıcı/DB hatası PR4'ün nötr sözleşmesiyle MASKELENMEZ, yapılandırılmış JSON 500 döner (nötr öneri geçerli bir kullanıcı durumudur; arıza onunla karıştırılamaz). schema_version YOK (bilinçli — bkz. docs/WEEKLY_PROGRAM.md F5; sözleşme yalnızca EKLEMELİ evrilir). Gözlemlenebilirlik: tek [TRAINING][WEEKLY_PROGRAM] debug satırı (has_data/week_focus/baseline var mı) — hacim/geçmiş/payload/PII loglanmaz. PR5'te şema/migration/koç-prompt/bayrak/UI değişikliği YOK — UI sınırı için alttaki PR6.1 maddesine bak. docs/WEEKLY_PROGRAM.md
- UI açılış sınırı (Sprint 6 PR6.1) — `WEEKLY_PROGRAM_UI_ENABLED` (app/config.py, varsayılan KAPALI, `== "1"` konvansiyonu). UI'ı üç PR'a bölünmüş açılışın İLKİ: PR6.1 yalnızca MONTAJ NOKTASI kurar — veri ÇEKMEZ, öneri RENDER ETMEZ (PR6.2 tüketici + durumlar + i18n; PR6.3 gözlemlenebilirlik/sertleştirme/dört-yönlü matris). Yüzey: `/training` sayfası, `#active-plan-view` içinde `#wstats` ile `.apv-meta-row` arasında (mevcut haftalık özet bölgesi; YENİ navigasyon hedefi YOK). KAPALI yol sunucu tarafında tamamen atlanır — markup/data-attribute/script/CSS/boşluk YOK; `{%- if %}`/`{%- endif %}` boşluk kontrolüyle çıktı PR6.1 ÖNCESİYLE BAYT-EŞ (normalize edilmiş before/after diff'iyle doğrulandı). AÇIKken tek atıl `<section id="weekly-program" data-weekly-program-mount aria-hidden="true">` + `static/weekly_program.js` (+153 bayt); görünür metin/locale anahtarı/öneri alanı/user id YOK. weekly_program.js kabuğu bulur, `data-weekly-program-initialized="1"` işaretler ve BİR KEZ çalışır (işaret DOM'da → yeniden değerlendirme çift-init yapamaz); fetch/XHR/listener/timer/eşik/hedef-aritmetiği YOK, `window.FitXWeeklyProgram.init` PR6.2'nin uzatma noktası. Bayrak SUNUM sınırıdır, YETKİ sınırı DEĞİL: endpoint her durumda @require_auth. AI_ADAPTIVE_PLAN_CONTEXT'ten BAĞIMSIZ (ayrı env adı, ayrı config anahtarı, ayrı okuma yolu). Route sayfaya YALNIZCA bu boolean'ı geçirir (build_weekly_program/WorkoutLog/planner çağırmaz — AST testli), ek SQL YOK. Geri alma: `WEEKLY_PROGRAM_UI_ENABLED=0`. docs/WEEKLY_PROGRAM.md
- app/services/workout_state/ — GÜNCEL antrenman-durumunun TEK kanonik sahibi (Sprint 7 PR1): mevcut kanonik kaynakları (en yeni TrainingPlan.plan_data programı → bugünün Istanbul günü/tip'i; bugünün PumpCheck'i = tamamlanma KANITI; marker yalnızca destekleyici; training_history üzerinden non-marker WorkoutLog satırları = YALNIZCA kayıtlı yürütme KANITI — aktif/kesintiye uğramış/DEVAM ETTİRİLEBİLİR bir seans DEĞİL; AI koç `commit_workout_log` bu satırları etkileşimli akış DIŞINDA yazar) deterministik, serileştirilebilir WorkoutStateSnapshot'a çevirir. execution_state {no_execution, execution_recorded, completed}; sunucuda kalıcı "başlatılmış seans" OLMADIĞI için sözleşmede `resume` action'ı YOKTUR — kanıt var ama tamamlanma doğrulanmamışsa action=none (asla uydurma resume). Tamamlanma TEK kanıtı PumpCheck: yalnız marker (PumpCheck'siz) tamamlanma SAYILMAZ → completion_marker_mismatch anomali, durum güvenli kalır. SALT-OKUNUR: yazma/flush/commit YOK, veri onarımı YOK, XP/challenge/bildirim YAN ETKİSİ YOK, AI/dış çağrı YOK. Saf/kirli ayrımı: queries.py=DB okuma → resolver.py=SAF sınıflandırma (ORM/Flask import ETMEZ → tüm durum matrisi DB'siz test edilir); __init__.resolve_workout_state orkestratör + arıza-güvenli (beklenmeyen hata → needs_attention/blocked, asla yanıltıcı rest/completed) + PII'siz `[WORKOUT_STATE] anomaly` logu. Beş boyut AYRI modellenir (schedule/execution/plan-relationship/action + TEK dominant primary_state) — tek aşırı-yüklü `completed` boolean'ına ÇÖKMEZ. Zaman dilimi app/timeutil'den (ikinci yardımcı YOK). plan↔log kimlik bağı YOK → ilişki yalnızca yerel-tarih+tip ile; kimlik-eşleşmesi türetilmez (belgelenmiş boşluk). Şema/migration/bayrak YOK; TEK converge edilen okuma yolu additive `GET /workout/status` (`completed` korunur + `state` eklenir). training.js renderHero + /api/progress/workout Sprint 7 PR4'te bu kanonik yola CONVERGE edildi (bkz. aşağıdaki PR4 maddesi). docs/WORKOUT_STATE.md
- app/services/workout_completion/ — DOĞRULANMIŞ antrenman-tamamlanmasının TEK kanonik MUTASYON sahibi (Sprint 7 PR2; PR1 salt-okunur read-model'in yazı tarafı). İki üretim tamamlanma yazıcısı buraya delege EDER: POST /workout/complete (training.py) ve AI koç _tool_analyze_gym_photo (ai_coach.py); ikisi de artık kendi tamamlanma semantiğini TAŞIMAZ (transport/doğrulama/medya/yanıt yalnız). KANIT-yalnızca yazıcılar (ai_coach _tool_confirm_and_commit_workout_log, fitx_mcp ham-SQL, manuel/wearable) bu yolu ASLA çağırmaz → PumpCheck/marker/tamamlanma-XP/challenge/bildirim ÜRETMEZ, PR1'de execution_recorded kalır. complete_workout(CompleteWorkoutCommand)->CompletionResult TEK atomik transaction'ın (commit/rollback) sahibi; çağrılan yardımcılar (award_xp/_claim_quest/log_activity/record_event/notify) commit ETMEZ (doğrulandı) — complete_quest_for_user gibi commit'leyen sarmalayıcılar KULLANILMAZ. Yan-etki sınıflaması: ATOMİK-zorunlu = PumpCheck + WORKOUT_COMPLETION_MARKER + görev + XP + Activity + arkadaş Message'ları (biri patlarsa hepsi rollback); best-effort in-tx savepoint = record_event challenge/rozet/bildirim/feed (kendi hatasını yutar, tamamlanmayı bloklamaz); yanıt-zenginleştirme = presigned URL + Redis LB after_commit (otorite bunlara BAĞLI DEĞİL). Tamamlanma KİMLİĞİ = user + Istanbul günü (date_key=app_today); yarış-güvenli TEK iddia uq_pump_check_day (migration YOK, bayrak YOK). already_completed_today salt-okunur preflight yalnızca replay'de pahalı Bedrock/S3'ü atlar (maliyet optimizasyonu, otorite DEĞİL). Yalnızca DOĞRULANMIŞ uq_pump_check_day ihlali ALREADY_COMPLETED'e eşlenir (non-500 replay); diğer her IntegrityError rollback + iç-hata olarak yükselir. Transaction içinde ağ (Bedrock/S3) çağrısı YOK. Zaman app/timeutil'den (ikinci yardımcı YOK). docs/WORKOUT_STATE.md "Sprint 7 PR2" bölümü + docs/handoff.md
- app/services/workout_session/ — KALICI, sunucu-sahipli antrenman-SEANSI yaşam döngüsünün TEK kanonik sahibi (Sprint 7 PR3). Sunucu artık bir seansın başladığını/aktif olduğunu/güvenle DEVAM ETTİRİLEBİLİR olduğunu/tamamlandığını/terk edildiğini/bayatladığını gerçekten yanıtlar (PR1 read-model'de kalıcı seans olmadığı için resume YOKTU). Saf/kirli ayrımı: models.py=frozen komut/sonuç + SessionOutcome enum + SAF sınıflandırma (ORM/Flask YOK) / queries.py=DB / service.py=transaction sahibi / __init__.py=public API. Durum makinesi ACTIVE/COMPLETED/ABANDONED; stale KALICI durum DEĞİL, ACTIVE'in TÜRETİLMİŞ koşulu. DB-düzeyi TEK atomik iddia: partial unique index uq_workout_session_active_owner (user_id WHERE status='active') — kullanıcı-başı EN FAZLA bir ACTIVE (PR2'nin uq_pump_check_day'ine paralel); is_active_session_owner_violation onu sınıflar, diğer IntegrityError fail-closed yükselir. public_id (secrets.token_urlsafe) TÜM API'de; sıralı id ASLA sızmaz. Kalp atışı (checkpoint) KİLİTSİZ koşullu UPDATE — yalnız last_activity_at, yalnız WHERE status='active', HEARTBEAT_COALESCE_SECONDS=30 içinde coalesce; replay ASLA sahte conflict döndürmez; satır-kilidi/version yalnız TERMINAL geçişlerde. Seans↔plan bağı YUMUŞAK referans (hard FK DEĞİL) + versiyonlu parmak izi v1:<sha256> (sunucu-tarafı, sıralı, istemciden ASLA); versiyon uyuşmazlığı → indeterminate (sessiz eşleşme YOK). BAYATLIK yalnız somut yaşam döngüsü/ilişki kanıtından (önceki gün/plan yok/yeniden üretilmiş/slot değişti/lifecycle tutarsız) — hareketsizlik ASLA otomatik terk/blok ETMEZ. Tamamlanma PR2 otoritesine delege eder; complete_workout linkli ACTIVE seansı AYNI transaction'da ACTIVE→COMPLETED terminalize eder (SABİT kilit sırası: seans satırı ÖNCE), yarış-kaybedeni dahil hiçbir yolda kalıcı ACTIVE bırakmaz, PumpCheck otoritesi olmadan COMPLETED yazmaz, artefakt ÇOĞALTMAZ. Bayrak FITX_WORKOUT_SESSIONS_ENABLED (varsayılan KAPALI): KAPALI ⇒ seans route'ları atıl (404) + resolver bayt-eş PR1 contract_version=1; AÇIK ⇒ tam yaşam döngüsü + contract_version=2 (session/session_state, resume/in_progress yalnız kalıcı UYGUN ACTIVE seanstan). Migration a994f9bed783 fail-closed verify-or-create (blanket-skip YOK). docs/WORKOUT_STATE.md "Sprint 7 PR3" bölümü + docs/handoff.md
- app/services/workout_state/ + `/training/bootstrap` — GÜNCEL antrenman-durumu TÜKETİCİ CONVERGENCE'ı (Sprint 7 PR4): tüm mevcut-durum tüketicileri TEK kanonik otoriteye (`resolve_workout_state`) ve TEK saf zarfa (`serialization.workout_state_payload` → `{completed, state}`) bağlanır — `/workout/status`, additive `GET /training/bootstrap`.workout ve `/api/progress/workout`.current BİREBİR aynı zarfı döner (Progress'in `days`/`totals` geçmiş alanları DEĞİŞMEZ, yalnız additive `current` eklenir). `training_bootstrap` İNCE kompozisyon katmanıdır: auth/tarih/bayrak/plan'ı sınırdan ÖNCE yakalar, `snapshot.coherent_read_snapshot()` ile TEK tutarlı okuma-transaction'ı açar (Postgres REPEATABLE READ; SQLite taze tx), `get_active_plan` + `resolve_workout_state(strict_reads=True)` + saf serileştiricileri BESLER, ARIZA-KAPALI'dır (herhangi bir okuma/şema/sınır hatası → tek jenerik `bootstrap_unavailable` 500 + `private, no-store`; ASLA kısmi/çelişkili veri, ham istisna, DB kimliği). `serialization.py` SAF ve sorgu-SUZ (ORM/Flask/DB import YOK); bozuk 7-günlük plan FAIL-CLOSED (`PlanSerializationError`), normal-yok durumu None/boş. `barcode.completed_today` + Coach `[GÜNCEL ANTRENMAN DURUMU]` bloğu artık aynı kanonik resolver'dan gelir — marker/log çıkarımı ya da ham plan/seans kimliği SIZMAZ (coach yalnız sınırlı `to_dict` projeksiyonu; `public_id` yalnız bayrak-AÇIK v2 opak token). `static/workout_state_client.js` sunucu-sahipli denetleyici: monotonik generation + `AbortController` bayat okumayı, tek-uçuş mutasyon + her mutasyon-sonrası SIRALI bootstrap yenilemesi save-ordering'i, `pagehide`/`pageshow(persisted)` + tek-sahip WeakMap teardown BFCache çiftlenmesini engeller; heartbeat yalnız görünür-sayfa v2 aktif-devam-ettirilebilir seans için tek 60sn timer. `training.js`'te tarayıcı-sahipli tamamlanma/tarih/seans/başlat/devam OTORİTESİ YOK (localStorage tamamlanma anahtarı + istemci `getTodayTurkish` KALDIRILDI). Şema/migration/model/yeni bayrak YOK; sözleşme additive (v1 bayrak-KAPALI bayt-eş, v2 kalıcı-seans). docs/WORKOUT_STATE.md "Sprint 7 PR4" + docs/handoff.md
- app/services/auth_contract.py — web ve mobil kimlik doğrulama sınırlarının ORTAK kararlarının TEK sahibi (Hardening PR3). İki istemci zaten TEK doğrulayıcıdan (cognito_jwt) geçiyordu; eksik olan, bu uyumu ZORLAYAN bir şeydi. Sözleşme şunlara sahiptir: istek doğrulamasında beklenen token_use (`access` — ID token bir kimlik iddiasıdır, API kimlik bilgisi DEĞİLDİR), her yolun cognito_jwt'ye geçirdiği expiry leeway, `TokenValidationError` için geçici/kesin ayrımı (`jwks_unavailable` = imza DOĞRULANAMADI → 503 + oturum KORUNUR; diğer her neden kesin ret → oturum YIKILIR) ve iki yolun ortak metrik sözlüğü (`AuthOutcomes`, Path×Outcome = 2×5, kullanıcı kimliği/token/başlık ASLA). Yanıt ŞEKLİ sözleşmeye DAHİL DEĞİLDİR: tarayıcı redirect/i18n metni, mobil ADR 0001'in `{code,message,retryable,request_id}` zarfını alır — bu fark BİLİNÇLİDİR ve testle sabitlenmiştir. TEK kasıtsız fark expiry leeway'di: web 0'a sabitken mobil `MOBILE_AUTH_VALIDATION_CLOCK_SKEW_SECONDS` (vars. 0, maks. 300) okuyordu. Bu ayar EMEKLİYE AYRILDI: istek doğrulamasında leeway İKİ YOLDA DA `REQUEST_CLOCK_SKEW_SECONDS=0`'dır ve yapılandırmadan OKUNMAZ (config'e yazılmaz da). Genelleştirilmedi — mobil adlı bir düğme tarayıcının süresi-dolmuş-token penceresini de genişletirdi; leeway bir ayar düğmesi değil güvenlik kararıdır, gerekirse İKİ yola birden uygulanan ayrı ve güvenlik-incelemeli bir değişiklik olur. Eski anahtar SESSİZCE YOK SAYILMAZ: `enforce_retired_settings` boot'ta KOŞULSUZ (MOBILE_AUTH_ENABLED'dan BAĞIMSIZ) kontrol eder; yok veya `=0` ise açılır, sıfırdan farklı/boş/sayısal olmayan değer boot'u durdurur (deploy health gate rollback yapar). Görünürlük: boot'ta `[AUTH_CONTRACT] ... skew=0s ... configurable=no` satırı + `/health?deep=1` içinde `auth_contract` bloğu. Göç ön koşulu: host `.env`'inden satırı SİL (docs/ROLLOUT.md §1). Drift kapıları (tests/test_auth_contract.py): `cognito_jwt.validate_token` yalnızca `VALIDATOR_CALL_SITES`'ta BEYAN EDİLEN modüllerden çağrılabilir (izin listesi kaydın kendisinden TÜRETİLİR, ikinci kopya yok — feature_flags'in `parsed_by` kuralının aynısı), `leeway_seconds` auth_contract DIŞINDA hiçbir yerde geçirilemez, joserfc yalnızca cognito_jwt'de import edilebilir (ikinci doğrulayıcı yok). docs/AUTH_CONTRACT.md
- app/services/mobile_nutrition/ + `GET /api/v1/nutrition/diary/today` — native istemcinin okuduğu TEK beslenme yüzeyi (Sprint 9 backend ön koşulu). İKİNCİ bir beslenme otoritesi DEĞİL, kanonik `MealLog` defterinin üzerine ADAPTÖR: diary builder'ı (`CustomMeal`/`CustomMealItem`) HİÇ sorgulamaz — iki toplam TOPLANAMAZ (çift sayım), bu yüzden yalnızca defter okunur. Eski web route'larına `@require_mobile_auth` EKLENMEDİ; eklenseydi belirsizlikleri (yılsız `DD.MM`, kimliksiz satır, naive timestamp, uydurma 0, sıralı DB id) mobil sözleşme olarak yayımlanırdı. Sınırda düzeltilenler: gün = tam ISO `YYYY-MM-DD` + IANA zone (yalnız `app/timeutil`; istemcinin `?day`/`?timezone`/`X-Timezone` girdisi YOK SAYILIR), `logged_at` = offset'li ISO 8601 (`to_app_tz`), NULL makro `null` kalır (ölçülmüş 0'dan ayrı), hedef = en yeni `UserSession.target_calories` ve NULL/pozitif-olmayan değer `goal: null` (0 = "ayarsız" bu sınırda BİTER; kolon değişmez), `ogun` Türkçe etiketi kararlı slot token'ına (`kahvalti/ogle/aksam/ara_ogun/unknown`) çevrilir, `source` bilinmiyorsa `manual`'a KATLANMAZ → `unknown`. Toplamlar sunucu-otoriter ve NULL'ı 0 sayar (mevcut davranış korunur, belgelenir; istemci yeniden hesaplamaz). Satır kimliği TÜRETİLİR, saklanmaz: `HMAC-SHA256(SECRET_KEY-alt anahtarı, "<user_id>\0<meal_log_id>")` → 24 karakter base64url, opak + sahibine bağlı (A'nın N satırı B'nin N satırı değildir) → MİGRATION YOK. Route mevcut `mobile_api` blueprint'ine takılır (tek `/api/v1` yüzeyi, tek no-store, tek 429, aynı MOBILE_AUTH_ENABLED kapısı, approved-route allow-list kapsamı sürer); tek yeni hata kodu `NUTRITION_TEMPORARILY_UNAVAILABLE` (depolama arızası bir kimlik doğrulama sonucu DEĞİLDİR — istemci sağlam oturumu atmasın). İstek başına 2 sınırlı SELECT, sağlayıcı çağrısı/kilit/yazma YOK; başarıda log YOK, arızada tek PII'siz satır. Web tarafı bayt-eş: hiçbir route, payload, alan adı, şema veya migration değişmedi. docs/MOBILE_NUTRITION.md
- app/services/plan_mutation/ — kanonik `TrainingPlan`'ın HEDEFLİ mutasyonunun TEK sahibi (Adaptive Coaching Sprint 1 PR1). Kural: **AI bir plan değişikliği İSTEYEBİLİR; plan kalıcılığına ASLA sahip olamaz** — karar bu sınırındır. Yüklenmiş bir satırın `plan_data`'sına atama YALNIZCA `service.py`'de yapılır (AST kapısı: tests/test_plan_mutation_architecture.py); `POST /training-plan/save` YENİ satır kurduğu için ayrı ve dokunulmamış bir yoldur (tüm-plan değiştirme; hedefli mutasyon DEĞİL). Sözleşme TİPLİdir (`commands.py` frozen dataclass'ları: replace/add/remove egzersiz, `set`+`tekrar` reçetesi, gün içeriği taşıma) — keyfi dict, ORM alan ataması, JSON Patch ya da jenerik "şu alanı yaz" YÜZEYİ YOK; tanınmayan komut satır OKUNMADAN reddedilir. SAHİPLİK YAPISALDIR, kontrol DEĞİL: hiçbir komut `user_id`/plan id TAŞIMAZ, servis çağıranın KENDİ aktif planını `user_id`'ye scope'layarak çözer → çapraz-kullanıcı mutasyonu ifade EDİLEMEZ; "plan yok" ile "başkasının planı" AYNI `PlanNotFound`'a çıkar (varlık sızmaz). HEDEFLİLİK doküman katmanında yaşar: plan TEK JSON text kolonu olduğundan her yazma kolonu baştan yazar, bu yüzden `document.apply_command` derin kopya alıp TEK düğüme erişir ve onu YERİNDE değiştirir — dokunulmayan gün/egzersizler ayrıştırılan AYNI nesnelerdir, dolayısıyla bu PR'ın modellemediği alanlar da korunur (`plan_facts` gibi projeksiyondan yeniden kurmak onları sessizce DÜŞÜRÜRDÜ). Türetilmiş plan-seviyesi değerler (`haftalik_ozet`) ASLA yeniden hesaplanmaz — tek egzersiz takasından haftalık özet üretmek bu sınırın olmayan planlama yetkisini uydurması olurdu. Sınırlar `training_generation/response_validator`'dan YENİDEN KULLANILIR (ikinci sabit kümesi YOK), ama duruş TERSTİR: üretici LLM çıktısını KIRPAR, mutasyon sınırı REDDEDER (999 set isteyene 100 yazmak olmayan bir isteği başarılı göstermek olurdu). Egzersiz kimliği `isim`'den ibarettir (ID YOK) → gün içinde birden çok eşleşme `AmbiguousExerciseTarget` ile reddedilir, konuma göre ASLA çözülmez; üretim egzersiz kataloğu bu PR'da UYDURULMAZ. Atomik: doğrula → `with_for_update` ile kendi aktif planını kilitle → kopyaya uygula → no-op ise plan METNİNİ YAZMA (deterministik `changed=False`, churn/regenerasyon YOK) → serialize/flush/commit; her arıza rollback, kısmi mutasyon YOK, transaction içinde ağ çağrısı YOK. Tamamlanmış geçmiş ŞEMA düzeyinde güvendedir: `WorkoutLog` kendi kolonlarını (`exercise_name`/`sets`/`reps`/`weight_kg`/`volume`) anlık görüntüler ve `plan_data`'dan hiçbir şey TÜRETMEZ → plandan geçmişe kod yolu yoktur. PR1'de route/AI aracı/bayrak/model/migration YOK ve hiçbir mevcut çağıran bu sınıra taşınmadı. Bilinen etkileşim: gün egzersizlerini değiştirmek Sprint 7 PR3 seans parmak izini değiştirir → bağlı ACTIVE seans `plan_regenerated_or_replaced` → stale olur; BİLEREK dokunulmadı (parmak izini tazelemek bu servisi workout-session durumunun İKİNCİ yazıcısı yapardı) — PR2 bu kararı UNDO yolunda da korur. **Sprint 1 PR2** kalıcı sürüm + mutasyon defteri + idempotent replay + güvenli geri alma ekler (migration `b3c4d5e6f7a8`, tek head; hâlâ route/AI aracı/bayrak YOK — defter API DEĞİL, DELİLdir). `TrainingPlan.mutation_version` YALNIZCA ARTAR: her kalıcı durum geçişinde +1, geri almada da +1; no-op/doğrulama hatası/replay/çakışma/commit hatasında DEĞİŞMEZ — geri alma ASLA azaltmaz (tek yazıcı `service.py`, AST kapılı). `TrainingPlan.lineage_id` (opak `token_urlsafe(32)`, satır default'u) plan SOYUNU adlandırır: `POST /training-plan/save` sil+ekle yaptığı için PK soyu taşıyamaz → yeniden üretilen plan otomatik YENİ soy alır ve geri almanın eski planın anlık görüntüsünü yenisine geri koyması YAPISAL olarak imkânsızdır. `PlanMutationRecord` defteri EKLEMELİdir: geri alma, ters çevirdiği satırı SİLMEZ/DEĞİŞTİRMEZ, `reverts_mutation_id` ile ona işaret eden YENİ satır ekler; anlık görüntüler kanonik `plan_data` METNİNİN TAM KENDİSİdir (projeksiyon/DTO/`plan_facts` DEĞİL — modellenmeyen alanları sessizce düşürürdü). `plan_lineage_id` ve `reverts_mutation_id` YUMUŞAK referanstır (hard FK legacy cascade'i denetim geçmişini yok ederdi). Idempotency: `MutationContext.idempotency_key` ZORUNLUdur ve kullanıcıya scope'ludur; aynı anahtar+aynı işlem → replay (kalıcı sonucun KENDİSİ döner, güncel durum DEĞİL), aynı anahtar+farklı işlem → `IdempotencyConflict` (fail-closed), farklı kullanıcı+aynı metin → bağımsız. YARIŞIN SON HAKEMİ VERİTABANIDIR: `uq_plan_mutation_user_key` (süreç belleği/Redis/zaman penceresi DEĞİL). Kabul edilen no-op da KALICI olarak kaydedilir (`outcome="no_op"`) — izsiz bırakılsaydı sonraki bir retry planı geriye çekebilirdi. Doğrulama hatası işlem kimliğini TÜKETMEZ. `undo_last_change` TEK kanonik ters işlemdir (redo/`rollback_to_version`/`restore_snapshot` YOK): aynı soyun en yeni, henüz ters çevrilmemiş `applied` mutasyonunu seçer, ÖN KOŞUL BAYTTIR (`snapshot_fingerprint(plan_data) == target.after_fingerprint` — sürüm eşitliği çok-seviyeli geri almayı KIRAR), uymazsa `UndoConflict` ile fail-closed; sezgisel ters komut/LLM YOK. `actor` (`user|ai_coach|system`) ve `reason` (≤200, kırpma YOK reddetme VAR) denetim verisidir, YETKİ DEĞİL. Paket LOGLAMAZ (logger/print/current_app yok — AST kapılı): anlık görüntü, komut, reason, egzersiz listesi, anahtar, parmak izi ASLA log'a çıkmaz. PG yarışları `tests/test_plan_mutation_history_pg.py` (CI `PostgreSQL concurrency` job'ı). docs/ADAPTIVE_COACHING.md
- app/services/coach_plan_tools/ — AI Koç ile plan-mutasyon sınırı arasındaki TEK köprü (Adaptive Coaching Sprint 1 PR3): bu depoda bir dil modelinin kullanıcı verisine KALICI yazma sebep olabildiği İLK yer. PR1/PR2 çağıransız gönderilmişti; çağıran budur. `ai_coach` `plan_mutation`'ı import ETMEZ, `coach_plan_tools` `ai_coach`'u import etmez — iki yön de YAPISAL kapılıdır (tests/test_plan_mutation_architecture.py + tests/test_coach_plan_tools_architecture.py); PR1'in "hiçbir koç modülü domain'i HENÜZ import etmiyor" testi SİLİNMEDİ, "yalnızca bu köprüden ulaşır"a DARALTILDI + kimin `plan_mutation` import edebileceği açık izin listesiyle sabitlendi. ALTI DAR araç (replace/add/remove egzersiz, `set`+`tekrar` reçetesi, gün taşıma, geri alma) — jenerik `mutate_training_plan(operation, payload)`/JSON Patch/`set_plan_field` YOK; jenerik araç, tipli sınırın ÜSTÜNDE keyfi bir mutasyon dili verip PR1'in sözleşmesini dekoratif yapardı (`operation/payload/patch/path/field/value/sql` adlı alan testte reddedilir). Bir alan şemada YALNIZCA kullanıcı NİYETİ taşıyorsa bulunur: `user_id`/`plan_id`/`lineage_id`/`mutation_version`/`mutation_id`/`actor`/`reason`/işlem anahtarı/anlık görüntüler/parmak izleri HİÇBİR şemada YOK (test yayımlanan şemaları gezerek doğrular); sınırlar `plan_mutation.validation`'dan import edilir — şema modeli YÖNLENDİREN UX'tir, kabul/ret otoritesi domain'de kalır. `parser.py` REDDEDER, kırpmaz/uydurmaz: bilinmeyen alan hataya ADIYLA girer (sessizce düşürmek, modelin İFADE ETTİĞİNDEN BAŞKA bir isteği çalıştırmaktır), eksik zorunlu alana varsayılan ATANMAZ (plan ÜRETİCİ boşluk doldurur çünkü biraz yanlış plan plansızlıktan iyidir; mutasyonda "3 set" uydurmak fabrikasyon TALİMATtır), `bool` `int`'ten ÖNCE reddedilir (yoksa `True` "1 set" olarak geçerdi), boş string reddedilir. Komut dal-başına ANAHTAR KELİMEYLE kurulur — model stringine dinamik dispatch ve `**arguments` splat'ı AST ile YASAK. Argüman reddi işlem kimliği TÜKETMEZ (aynı turdaki düzeltilmiş deneme taze işlemdir). Tur kimliği YENİ bir sistem DEĞİL, var olan sunucu-üretimi `g.request_id`'dir (istemciden enjekte EDİLEMEZ, zaten log/SSE korelasyon kimliğidir, sağlayıcı retry'ını + Bedrock→OpenAI geçişini + tüm araç-devam turlarını atlatır). İşlem anahtarı `aic:{request_id}:{sha256(domain\0tur\0komut_tipi\0semantic_fingerprint)[:16]}`: HAM MODEL JSON'U HASH'LENMEZ (boşluk/anahtar sırası/yankılanan tek alan, tek niyet için taze anahtar üretirdi) ve OLUŞUM SAYACI YOKTUR (sayaç, tek mutasyonun ikinci TESLİMİNİ yeni işlem yapardı = tam da engellemek için var olduğu çift `add`/çift undo). Garanti TEK sunucu turuyla SINIRLIDIR ve öyle BELGELENİR: yeni HTTP isteği yeni `request_id` = yeni niyettir (iki mesajda iki kez isteyen kullanıcı gerçekten iki kez istemiştir); taşıma-düzeyi exactly-once İDDİA EDİLMEZ. `MAX_PLAN_OPERATIONS_PER_TURN=2`, `g` üzerinde istek-kapsamlı, `_COACH_TOOL_LOOP_CAP`'in (5) KESİN ALTINDA — döngü tavanı "kaç araç çağrısı"nı sınırlar, kalıcı YAZMA sayısını değil; tavana eşit/üstü bütçe dekoratiftir. TEKRARLANAN anahtar bütçe HARCAMAZ (harcasaydı zaten uygulanmış bir düzenleme kullanıcıya "çok fazla değişiklik" diye REDDEDİLMİŞ görünürdü — mevcut en kötü yanıt) ve bütçe reddi önceki düzenlemeleri GERİ ALDIRMAZ. `actor="ai_coach"` sunucu-sahiplidir, hiçbir şemada geçmez ve DENETİM verisidir — YETKİ DEĞİL (çapraz-kullanıcı mutasyonu PR1'de zaten ifade edilemez); `reason=None` (ham kullanıcı metni kalıcı denetim kolonunda güvenilmez girdidir, model metni modelin YAPTIĞINI SANDIĞININ doğrulanmamış anlatısıdır, sabit "AI Coach" string'i `actor` kolonunun söylemediği hiçbir şeyi söylemez); ikisi de TEK `MutationContext(...)` çağrı yerinde AST ile sabitlenir. Araç SONUCU bir sonraki model çağrısının GİRDİSİDİR (faturalanır ve kullanıcıya aktarılabilir) → kapalı ve küçüktür: `applied|no_op|replayed|error` + 16 kod, her koda sunucu-yazımı kurtarma talimatı İLİŞTİRİLİR (kurtarma politikası sistem promptuyla yarışmaz, tarif ettiği arızayla BİRLİKTE gider); kodlar istisna SINIFINDAN eşlenir → domain metni değişince sözleşme değişmez. Anlık görüntüler/plan metni/`lineage_id`/satır kimliği/işlem anahtarı/parmak izi/SQL/ham istisna ASLA geçmez; `mutation_id` de geçmez (model onunla bir şey YAPAMAZ, ama bağlam penceresindeki kimlik kullanıcıya yankılanabilen kimliktir). `replayed` `applied`'dan AYRIDIR — model "bu zaten yapıldı" diyebilsin, olmayan ikinci değişikliği ANLATMASIN; `no_op` da açıkça söyler. Uygulanan her sonuç, prompt'taki plan bloğunun artık BAYAT olduğunu bildiren bir not taşır (blok tur ortasında YENİDEN RENDER EDİLMEZ: pencereye planın İKİNCİ bir tarifini koyardı ve sonraki çağrıdan sonra yine bayat olurdu). Geri alma ARGÜMANSIZ ve TEK araçtır (`undo(version)`/`rollback_to`/redo YOK; PR2'nin `undo_last_change`'i kanonik terstir, ön koşulu BAYTtır): `undo_fingerprint()` SABİT olduğundan bir turdaki her undo AYNI işlemdir → çift teslim, geriye uzanıp İKİNCİ (daha eski) bir değişikliği ters çevirmez, ilkini replay eder; argüman YOK SAYILMAZ, REDDEDİLİR. Sağlayıcı arızası KALICI mutasyonu geri aldırmaz: bir plan aracı koştuktan sonra B-kuralı Bedrock→OpenAI geçişini YASAKLAR (ikinci sağlayıcı, zaten değişmiş plana karşı turu yeniden koşar ve bunu bilemez), tur yumuşak hataya düşer ve mutasyon COMMIT'Lİ KALIR — bu durumda kullanıcıya gösterilen metin de DEĞİŞİR: jenerik "işlemi tamamlayamadım, tekrar dener misin?" hem yanlıştır hem de tekrar denemeye çağırdığı için yeni istek/yeni request_id/yeni işlem kimliği üzerinden aynı düzenlemeyi İKİNCİ kez uygulatır; bunun yerine "plan değişikliğin kaydedildi, ama yanıtı tamamlayamadım" denir (bloklayıcı yolda `_coach_tool_fallback`, akışta `coach.reply_failed_plan_saved` SSE anahtarı; işaret `g` üzerinde istek-kapsamlı ve YALNIZCA applied/replayed için kurulur — no_op ve her ret eski metni korur; metin hâlâ hata-yedeğidir, kota iadesi ve B16 davranışı DEĞİŞMEZ). `executor._settle_transaction()` araçtan sonraki sağlayıcı çağrısının ÜZERİNDEN transaction TAŞINMAMASINI garanti eder, ama YALNIZCA kanıtlanabilir salt-okunur artığı geri alır — bekleyen iş sahibine bırakılır (blanket rollback ikinci bir transaction otoritesi olurdu). Aynı gerekçeyle PR2'nin bilinen Minor'ı burada KAPATILDI: `service.py`'nin `IntegrityError` hakemlik dalı rollback sonrası defteri yeniden okuyup o okumayı AÇIK bırakıyordu; çağıran yokken belgelenmiş artıktı, hemen aşağısında bloklayıcı sağlayıcı çağrısı varken ağ I/O'su boyunca tutulan transaction olurdu (`_arbitrated_result` artık `finally` ile kapatır). Bayrak `AI_COACH_PLAN_MUTATION_TOOLS_ENABLED` (kanonik `app/feature_flags.py::ROLLOUT_FLAGS` kaydı, vars. KAPALI, `staging_only`, `depends_on=("AI_ADAPTIVE_PLAN_CONTEXT",)`, `current_app.config`'ten okunur, arıza-KAPALI) HER İKİ yarıyı da kapatır: KAPALI ⇒ araçlar İKİ sağlayıcı şemasından da ÇIKAR *ve* yürütme reddedilir (önceki turdan adı hatırlayan model domain'e ULAŞAMAZ) + politika bloğu prompt'a EKLENMEZ (prompt, modelin kullanamayacağı bir yeteneği TARİF ETMEMELİDİR). Geri alma yeni AI yazmalarını durdurur, UYGULANMIŞ mutasyonları geri ALMAZ (onlar sürümlü plan geçmişidir; kullanıcı undo edebilir) — gözlem penceresinin abort sinyali oran değil TEKİL olaydır (docs/ROLLOUT.md #8). Model politikası TEK yerde yaşar (`app/prompts/system.py::PLAN_MUTATION_POLICY`): açık kullanıcı isteği ŞARTTIR, tavsiye/varsayım/kendi önerisini kendi uygulaması DEĞİLDİR, yorgunluk/plato/AdaptivePlan sinyalleri yazma YETKİSİ VERMEZ, belirsiz hedef TAHMİN EDİLMEZ (sorulur), tam program yeniden tasarımı bu araçların işi değildir. Gözlemlenebilirlik TEK PII'siz satırdır: `[COACH][PLAN_TOOL] request_id=... tool=... outcome=...` (iki değer de kapalı sunucu sözlüğünden; AST testi format string'i DEĞİL çağrının ARGÜMANLARINI kısıtlar — format sunucu sabitidir, kullanıcı/model verisi argümandan girer); METRİK YOK (bu yeteneğin dürüst sinyali — "AI kullanıcının istemediği bir şeyi mi değiştirdi?" — sayılabilir değildir; sayılabilirmiş gibi davranan sayaç hiç yoktan kötüdür), otoritatif kayıt `PlanMutationRecord` defteridir. PR3'te YENİ route/şema/migration (Alembic head hâlâ `b3c4d5e6f7a8`)/mobil/Flutter değişikliği, onay-önizleme UX'i, etki sınıflandırıcısı ve proaktif düzenleme YOK; mevcut 8 koç aracı bayrak AÇIK ve KAPALIYKEN bayt-eş davranır; `WorkoutSession`/`WorkoutLog` satırları plan düzenlemesi boyunca DEĞİŞMEZ (bağlı ACTIVE seansın bayatlaması PR1/PR2'deki BİLİNÇLİ karardır — parmak izini tazelemek bu sınırı workout-session durumunun ikinci yazıcısı yapardı). docs/ADAPTIVE_COACHING.md §§19-30 + docs/handoff.md
- nutrition_pipeline.py, analytics_engine.py — deterministik makro değerlendirme / nudge motoru
- s3_helper.py — S3 görsel yükleme + pre-signed URL (EC2 IAM Instance Profile ile auth; AWS anahtarı hardcode YOK)
- migrations/ — Alembic (Flask-Migrate) şema geçmişi
- templates/ + static/ — Türkçe UI (index, nutrition, training, progress, setup, friends, chat, quests, leaderboard, ...)
- Dockerfile / docker-compose.yml — web (gunicorn, tek worker/8 thread) + redis; Postgres artık compose içinde değil, prod'da external RDS/DATABASE_URL ile gelir; servisler loopback'e bağlı
- nginx.conf — host reverse proxy: / → Flask:5000, /fatsecret/rest/server.api → loopback proxy (127.0.0.1:3000 — ayrı süreç DEĞİL, host nginx'in kendi server bloğu; süpervizyon = nginx systemd; ayrıntı/geçmiş: deploy/fatsecret-proxy.md — deploy.yml dinleyiciyi kontrol eder, /health?deep=1 raporlar)
- tests/ — pytest (menü çıkarımı, makro alaka, nutrition pipeline)
- .env — SECRET_KEY, DATABASE_URL, FATSECRET_*, OPENAI_API_KEY, OPENAI_MODEL, BEDROCK_*, COGNITO_*, AWS_REGION, S3_BUCKET_NAME, REDIS_URL, RESEND_API_KEY, EMAIL_FROM_NAME/EMAIL_FROM_ADDRESS/EMAIL_REPLY_TO (commit etme)
  - Örnek için .env.example'a bak. OpenAI anahtarı .env'den okunur, asla hardcode edilmez.
  - Opsiyonel güvenlik/freemium/gözlem anahtarları (hepsinin makul varsayılanı var):
    - `AI_PLAN_QUOTA_ENABLED` (vars. 1) — non-premium'a haftada 1 AI plan üretimi (app/services/premium.py). 0 = kota kapalı.
    - `LOGIN_FAIL_CLOSED` (vars. 1) — Redis erişilemezse login 503 (brute-force throttle güvenilir değilken). 0 = eski fail-open.
      Bilinçli tradeoff (A5): Redis availability == login availability. Redis tek konteynerdir;
      diğer her şey degrade eder (session=cookie, leaderboard→Postgres, foodcache→L1,
      limiter→in-memory), yalnızca login 503 olur. `/health?deep=1` login offline'ken 503
      döner ve deploy gate bunu kullanır (I2); sığ /health liveness için yeşil kalır.
    - `AI_MAX_CONCURRENCY` (vars. 4) + `SCRAPE_MAX_CONCURRENCY` (vars. 2) +
      `AI_MODEL_MAX_CONCURRENCY` (vars. AI_MAX_CONCURRENCY) +
      `AI_GATE_WAIT_SECONDS` (vars. 0) — ağır AI/scrape route'larında eşzamanlılık
      tavanı (app/services/ai_gate.py); dolunca 503 + Retry-After. Thread rezervi
      /health ve ucuz route'ları AI yükünden korur (A1/I1).
      **Hardening PR4** rezervi belgeden KODA taşıdı — TEK kaynak `docs/CAPACITY.md`:
      (1) boot invariant'ı artık model kapısının dış kapıyı AŞAN kısmını
      (`max(0, AI_MODEL_MAX - AI_MAX)`) ve DB havuzunu da sayar
      (`DB_POOL_SIZE + DB_POOL_MAX_OVERFLOW >= FITX_WEB_THREADS`); üretimde ihlal
      loglanmaz, boot'u DURDURUR (deploy health gate rollback yapar).
      (2) DB havuzu artık AÇIK: `DB_POOL_SIZE` (vars. FITX_WEB_THREADS),
      `DB_POOL_MAX_OVERFLOW` (vars. 4), `DB_POOL_TIMEOUT_SECONDS` (vars. 10) —
      önceden SQLAlchemy örtük varsayılanları (5/10/**30 sn sessiz bekleme**)
      yürürlükteydi ve havuz thread sayısının ALTINDAYDI.
      (3) Rezerv ÖLÇÜLÜR: `ThreadReserve`/`AiSlotsActive`/`AiModelSlotsActive`/
      `ScrapeSlotsActive` gauge'ları flush thread'inden yayılır (`/health?deep=1`
      yolundan DEĞİL — o uç düzenli poll edilmiyor, alarmlar INSUFFICIENT_DATA'da
      kalıyordu). `ThreadReserve` PR4 öncesi HİÇ yayınlanmıyordu, oysa ROLLOUT.md
      ve MOBILE_AUTH_ENABLED kaydı onu rollout abort sinyali olarak adlandırıyordu.
      (4) Giyilebilir sağlayıcı route'ları (`callback`/`sync`/`whoop`) artık
      PAYLAŞILAN `blocking_concurrency_slot`'u tüketir — çağrı başına ~30 sn'ye
      kadar thread park eden bu yüzey rezerv aritmetiğinde SAYILMIYORDU (PR #199'un
      mobil Cognito uçları için kapattığı boşluğun aynısı, aynı mekanizmayla).
      (5) Bilinmeyen `kid` için zorunlu JWKS tazelemesi TEK-UÇUŞ + soğuma
      penceresi altındadır (`JWKS_FORCED_REFRESH_COOLDOWN_SECONDS`, vars. 60):
      koşulsuzken uydurma kid taşıyan istek seli her istek başına bir thread'i
      5 sn park ettirebiliyordu. Kanıtlar: `tests/test_capacity_invariants.py`.
    - `MAX_CONTENT_LENGTH_BYTES` (vars. 12 MiB) — GENEL istek-gövdesi tavanı
      (app/config.py). Flask varsayılanı SINIRSIZDIR ve alan-başı kapılara
      (avatar 500 KB, pump-check 8 MB data-URL) ulaşılmadan önce gövde zaten
      okunur. Tavan en büyük meşru yükün ÜSTÜNDE tutulur — kanonik sınır
      alan-başı doğrulayıcılar olmayı sürdürür, bu yalnızca akıl-sağlığı
      tavanıdır. Aşım JSON 413 döner; mobil yüzeyde `REQUEST_TOO_LARGE` +
      retryable=False (blueprint'in genel kancası bunu retryable bir arıza
      sanmasın). `/chat` ayrıca `MAX_QUESTION_CHARS` girdi tavanını `/ask*` ile
      AYNI şekilde uygular — rate limit istek SAYISINI sınırlar, istek-başı
      token MALİYETİNİ değil.
    - `SENTRY_DSN` (yoksa kapalı) + opsiyonel `SENTRY_ENVIRONMENT`, `SENTRY_TRACES_SAMPLE_RATE` — hata izleme (app/observability.py). DSN yoksa no-op.
    - `RUNTIME_METRICS_ENABLED` (vars. 0) + `RUNTIME_METRICS_NAMESPACE` (vars.
      `FitX/Runtime`) + `RUNTIME_METRICS_FLUSH_SECONDS` (vars. 60) — üretim
      runtime SLI'ları (app/services/runtime_metrics.py). `AI_METRICS_ENABLED`in
      KARDEŞİ ve ondan BAĞIMSIZ (ayrı namespace, tek başına geri alınır); aynı
      `cloudwatch:PutMetricData` iznini ister. KRİTİK TASARIM: kayıt istek
      yolunda SÜREÇ-İÇİ TAMPONA yazılır, CloudWatch'a yalnızca daemon flush
      thread'i çıkar — `put_metric_data` bir ağ çağrısıdır ve satır-içi
      çağrılsaydı tek worker/8 thread'lik kapasiteye istek başına bir round-trip
      eklerdi (ölçmeye çalıştığı gecikmeyi bozardı). Gecikmeler kova histogramı
      olarak `Values`/`Counts` ile gider (StatisticSet yüzdelik veremez → p95
      hesaplanamazdı). Boyutlar SABİT kümeli: blueprint/status-sınıfı/istemci-
      sınıfı/sağlayıcı/sonuç; kullanıcı kimliği, ham path ve token ASLA. İstemci
      sınıfı (`web`/`mobile`) `request.blueprint`'ten türetilir, istemci
      başlığından ASLA. 503 (`HttpOverload`, kasıtlı yük atma) ile 500
      (`HttpServerErrors`, kusur) AYRI sayaçlardır — birleştirilirse sağlıklı
      yük atma alarm üretirdi. docs/OBSERVABILITY.md
    - Rollout bayrakları host `.env`'inde yaşar ve deploy pipeline'ı onları
      TAŞIMAZ (`git reset --hard origin/main` `.env`'e dokunmaz) → "canlıda hangi
      bayrak açık?" sorusunun repodan yanıtı YOKTUR. İki görünürlük yolu:
      boot'ta tek `[FLAGS] enabled=...` satırı ve `/health?deep=1` içindeki
      `flags` + `capacity` blokları (yalnızca ad+boolean; iç-ağ sınırlı, M3).
      Envanter: `app/config.py` `FEATURE_FLAG_KEYS` / `feature_flag_state()`.
  - Operasyon notu: web konteyneri hâlâ tek gunicorn worker + 8 thread çalışır.
    Coach/menu/plan AI çağrıları senkron ve bloklayıcıdır; 8 uzun AI isteği tüm
    uygulama thread'lerini doldurabilir. Worker sayısını artırmadan önce in-memory
    cache/limiter fallback varsayımlarını kaldır veya AI uçlarını ayrı worker/queue'ya taşı.

## Veritabanı
Lokal: SQLite (instance/chatbot.db). Prod/Docker: PostgreSQL (DATABASE_URL ile).
Beslenme TEK kanonik defterde tutulur: MealLog (UI + AI koç + menü). Eski
UserDailyNutrition verisi MealLog'a taşındı ve tablo düşürüldü (migration
f6a7b8c9d0e1). MealLog.tarih ISO 'YYYY-MM-DD' (Istanbul); gün anahtarları için
app/timeutil kullan.
Tablolar boot'ta db.create_all() ile oluşur (app/db_init.py); ayrıca Alembic baseline migration mevcut.
Şema değişikliği akışı (yeni değişiklikler için tercih edilen yol):
1. Modeli app/models.py'de değiştir
2. `FITX_SKIP_DB_INIT=1 flask --app starter db migrate -m "açıklama"`
3. Üretilen dosyayı gözden geçir ve commit'le — uygulama boot'ta bekleyen
   migration'ları OTOMATİK uygular (app/db_init.py); alembic_version tablosu
   olmayan eski DB'ler de boot'ta otomatik `stamp` ile zincire alınır.
   Manuel `flask db upgrade` / `stamp head` yalnızca lokal işler için gerekir.
Modeller: User, UserSession, WeeklyLog, WeeklyCheckIn, NutritionPlan, TrainingPlan, MealLog,
PendingAction, CoachConversation, CoachMessage, PumpCheck, Friendship, Message, Activity,
Supplement, DailyQuest, UserQuestProgress, WeeklyWinner, WeeklyResetLog, WaterLog, WorkoutLog,
DailyActivity, CustomMeal, CustomMealItem, Notification, FeedItem, FeedItemLike,
FeedItemComment, FeedHide, FeedReport (bu 5 = Sprint 5 PR2 Feed V2), Challenge,
UserChallengeProgress, UserBadge (bu 3 = Sprint 5 PR3 Challenges; Challenge katalog,
user_id YOK → cascade listesinde değil; UserChallengeProgress+UserBadge çocuk),
WorkoutSession (Sprint 7 PR3 = kalıcı antrenman-seansı yaşam döngüsü; user-child,
partial unique index uq_workout_session_active_owner ile kullanıcı-başı EN FAZLA bir
active satır),
PlanMutationRecord (Adaptive Coaching S1 PR2 = plan mutasyon defteri; user-child,
uq_plan_mutation_user_key yarışın son hakemi, uq_plan_mutation_reverts bir
mutasyonun EN FAZLA bir kez geri alınmasını garanti eder; TrainingPlan'a hard FK
YOK — legacy sil+ekle cascade'i denetim geçmişini yok ederdi)
Yeni user-child model eklerken app/cli.py `_user_child_models` listesine de ekle
(tests/test_cascade_delete.py introspeksiyonla doğrular).
DİKKAT (taze DB boot yolu): app/db_init.py önce db.create_all() çalıştırır, sonra
aa11bb22cc33'ü damgalayıp head'e upgrade eder — yani o revision'dan SONRAKİ her
migration create_all'ın zaten kurduğu şemaya karşı da koşar. Tablo yaratan yeni
migration'lar bu yüzden TEKRAR-ÇALIŞTIRILABİLİR olmalı (bkz. cc33dd44ee55:
`sa.inspect(op.get_bind()).has_table(...)` kapısı).

## Kurallar
- Kısa commit mesajları yaz
- Türkçe UI, İngilizce kod
- Test: `pytest` + `flask run` ile local test et
- CSP: başlık Flask'ta üretilir (app/hooks.py). Şablona yeni satır-içi <script> eklerken
  MUTLAKA `<script nonce="{{ csp_nonce }}">` yaz, yoksa tarayıcı bloklar. Dış script
  yalnızca cdn.jsdelivr.net ve *.googletagmanager.com'dan (GA) yüklenebilir.
- CSRF: tüm POST/PUT/PATCH/DELETE iki katmandan geçer (app/hooks.py `_csrf_protect`):
  Origin/Referer + per-session synchronizer token. Token `<meta name="csrf-token">`
  (_head.html, `csrf_token` context processor) ile verilir; static/csrf.js `window.fetch`'i
  sararak durum-değiştiren aynı-origin fetch'lere `X-CSRFToken` başlığını OTOMATİK ekler.
  Yeni fetch çağrısı ek iş istemez; AMA: (1) yeni sayfa `_head.html`'i include etmeli,
  (2) durum-değiştiren bir istek fetch DIŞINDA (XHR/sendBeacon) yapılıyorsa X-CSRFToken
  başlığını elle ekle, (3) state-changing route'u GET olarak açma (kapı yalnızca yazma
  metodlarında çalışır).
- Deploy: push to main → **ci.yml (pytest + şema-drift) YEŞİL olursa** →
  .github/workflows/deploy.yml (`workflow_run` ile CI'a kapılı) AWS SSM ile EC2'de
  compose'u yeniden kurar, /health?deep=1 gate'inden geçirmezse önceki commit'e
  rollback eder. CI kırmızıysa deploy HİÇ başlamaz (H3).
  NOT: main branch koruması ayrıca GitHub ayarlarından açılmalıdır — buradaki gate
  DEPLOY kapısıdır, branch koruması MERGE kapısıdır.
  Host nginx'te eski `add_header Content-Security-Policy` satırı kalırsa deploy
  canlı config'i sed ile değiştirmez; fail-fast yapar.
  DİKKAT (A2): rollback yalnızca KODU geri alır — boot'ta otomatik uygulanan DB
  migration'ları geri ALMAZ. Migration'ları expand/contract (geriye uyumlu) yaz:
  kolon/tablo DÜŞÜRME veya RENAME, eski kod en az bir başarılı deploy boyunca onsuz
  çalışabiliyorsa gönderilir. Yıkıcı migration kaçınılmazsa: önce RDS snapshot al,
  `FITX_DB_AUTO_UPGRADE=0` ile boot-upgrade'i kapat ve migration'ı ayrı tek seferlik
  `flask db upgrade` adımı olarak çalıştır. Boot'ta migration hatası artık FATAL'dir
  (app/db_init.py; kaçış: FITX_DB_UPGRADE_FAIL_OPEN=1) — health gate rollback yapar.
- Sorgular daima current_user.id'ye scope'lanır; ID ile yüklenen kayıtlarda sahiplik kontrolü zorunlu
- Gün-anlamlı challenge hunileri (`water_logged` gibi) KALICI bir gün-başı
  işaretle dedup edilir — `challenges.record_event` per-day dedup YAPMAZ, dedup
  çağıranın sorumluluğudur (`active_day` deseni). Kullanıcının değiştirebildiği
  bir sayıya ("0→pozitif geçiş") bakan kapı yazma: sayıyı sıfırlamak geçişi
  yeniden üretir ve "N GÜN" challenge'ı tek günde tamamlanır. Kanonik örnek:
  `WaterLog.quest_fired` + koşullu UPDATE (`training.py:_claim_water_funnel_for_today`).
- Bloklayıcı sağlayıcı çağrısı (FatSecret/Cognito/wearable/model) eklerken
  `blocking_concurrency_slot()` ZORUNLU: tek gunicorn worker + 8 thread; kapısız
  senkron ağ turu /health'i kuyruğa sokup yanlış deploy-rollback tetikler. Slot
  YALNIZCA ağ turunu sarsın — cache okuması/DB yazımı izin tutulurken yapılmaz.
  Kapasite reddi (`BlockingConcurrencyLimit`) 503 + Retry-After'a çevrilir,
  "sonuç bulunamadı"ya ASLA.
